# Copy to .env.local for local development outside Docker. Inside
# docker-compose, this is baked in at build time (see the Dockerfile) since
# Vite env vars are resolved at build, not at container start.
VITE_API_BASE_URL=http://localhost:8080
